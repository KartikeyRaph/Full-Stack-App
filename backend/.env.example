# Backend configuration
# HOST: IP address to bind (e.g., 0.0.0.0 to listen on all interfaces)
HOST=127.0.0.1
PORT=8000
# FRONTEND_URL: optional; set to the frontend origin for strict CORS (e.g., http://192.168.1.10:5173)
FRONTEND_URL=http://localhost:5173
